<%= form_for(@bill) do |f| %>
  <% if @bill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill.errors.count, "error") %> prohibited this bill from being saved:</h2>

      <ul>
      <% @bill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :billee_id %><br />
    <%= f.number_field :billee_id %>
  </div>
  <div class="field">
    <%= f.label :total %><br />
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :ordered %><br />
    <%= f.date_select :ordered %>
  </div>
  <div class="field">
    <%= f.label :billed %><br />
    <%= f.date_select :billed %>
  </div>
  <div class="field">
    <%= f.label :paid %><br />
    <%= f.date_select :paid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
